Страницы:
1. /calculate
2. /admin

Запуск: ./gradlew bootRun
Сборка: ./gradlew buildRelease

Ответ: Нужно добавить слой для работы с БД. У меня это реализовано с помощью щаблона Repository. Тут непонятно какая изначально берется архитектура. Относительно чего меняем. Если изначально был просто контроллер который тут же считал формулу и сразу выдавал веб страницу с ответом, тогда все надо менять. Архитектура этого проекта:
 1. Изолированные модули по фичам  
 2. Фреймворкнезависимыe домены
 3. Каждый модуль поделен на слои

Фреймворкнезависимыe домены позволят легко импортровать их в другие Gradle проекты (e.g. Android).
Изолированные модули позволят облегчть параллельную их разработку что ускорит развитие проекта в целом 